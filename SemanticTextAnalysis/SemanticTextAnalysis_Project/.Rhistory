{
for (k in 1:length(newsArticles[[1]])) # loop through all words in article j
{
#if (allWords[i] == newsArticles[[c(j,k)]])
#cat("I found word ", allWords[i]);
newsArticles[[c(j,k)]]
}
}
}
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
for (k in 1:length(newsArticles[[1]])) # loop through all words in article j
{
#if (allWords[i] == newsArticles[[c(j,k)]])
#cat("I found word ", allWords[i]);
#allWords[i]
k
}
}
}
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
for (k in 1:length(newsArticles[[j]])) # loop through all words in article j
{
#if (allWords[i] == newsArticles[[c(j,k)]])
#cat("I found word ", allWords[i]);
allWords[i]
}
}
}
;
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
for (k in 1:length(newsArticles[[j]])) # loop through all words in article j
{
#if (allWords[i] == newsArticles[[c(j,k)]])
#cat("I found word ", allWords[i]);
allWords[i]
}
}
}
;
length(allWords)
length(newsArticles)
length(newsArticles[[1]])
newsArticles[[c(1,2)]]
length(newsArticles)
length(newsArticles)
length(allWords)
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
for (k in 1:length(newsArticles[[j]])) # loop through all words in article j
{
if (allWords[i] == newsArticles[[c(j,k)]])
cat("I found word ", allWords[i]);
}
}
}
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
for (k in 1:length(newsArticles[[j]])) # loop through all words in article j
{
if (allWords[i] == newsArticles[[c(j,k)]])
#cat("I found word ", allWords[i]);
allWords[i]
}
}
}
;
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
for (k in 1:length(newsArticles[[j]])) # loop through all words in article j
{
if (allWords[i] == newsArticles[[c(j,k)]])
#cat("I found word ", allWords[i]);
allWords[i];
}
}
}
;;
;
asdasd
a = 2;
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
for (k in 1:length(newsArticles[[j]])) # loop through all words in article j
{
if (allWords[i] == newsArticles[[c(j,k)]])
#cat("I found word ", allWords[i]);
allWords[i];
}
}
}
;
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
for (k in 1:length(newsArticles[[j]])) # loop through all words in article j
{
if (allWords[i] == newsArticles[[c(j,k)]])
#cat("I found word ", allWords[i]);
length(allWords);
}
}
}
;
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
for (k in 1:length(newsArticles[[j]])) # loop through all words in article j
{
if (allWords[i] == newsArticles[[c(j,k)]])
cat("I found word ", allWords[i]);
#length(allWords);
}
}
}
allWords[2]
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
for (k in 1:length(newsArticles[[j]])) # loop through all words in article j
{
if (allWords[i] == newsArticles[[c(j,k)]])
#cat("I found word ", allWords[i]);
#length(allWords);
allWords[i];
}
}
}
newsArticles[[c(1,2)]]
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
for (k in 1:length(newsArticles[[j]])) # loop through all words in article j
{
if (allWords[[i]] == newsArticles[[c(j,k)]])
cat("I found word ", allWords[i]);
}
}
}
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
for (k in 1:length(newsArticles[[j]])) # loop through all words in article j
{
if (allWords[[i]] == newsArticles[[c(j,k)]])
#cat("I found word ", allWords[i]);
allWords[[i]]
}
}
}
allWords[1]
help is.element
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
if (is.element(allWords[i], newsArticles[j]))
cat("I found word");
#for (k in 1:length(newsArticles[[j]])) # loop through all words in article j
#{
# if (allWords[[i]] == newsArticles[[c(j,k)]])
#cat("I found word ", allWords[i]);
#  allWords[[i]];
#}
}
}
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
if (is.element(allWords[i], newsArticles[j]))
cat("I found word");
#for (k in 1:length(newsArticles[[j]])) # loop through all words in article j
#{
# if (allWords[[i]] == newsArticles[[c(j,k)]])
#cat("I found word ", allWords[i]);
#  allWords[[i]];
#}
}
}
a = 1;
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
if (is.element(allWords[i], newsArticles[j]))
a++;
}
}
a = 1;
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
if (is.element(allWords[i], newsArticles[j]))
a += 1;
}
}
a = 1;
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
if (is.element(allWords[i], newsArticles[j]))
{
a = a+ 1;
}
}
}
a = 1;
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
if (is.element(allWords[[i]], newsArticles[[j]]))
{
a = a+ 1;
}
}
}
a = 1;
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
if (is.element(allWords[i], newsArticles[[j]]))
{
a = a+ 1;
}
}
}
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
if (is.element(allWords[[i]], newsArticles[[j]]))
cat("Found");
}
}
index = list(rep(c(), times=600))
index = list(rep(listc(), times=600))
index = list(rep(list(c(), times=600))
index = list(rep(list(c(), times=600)));
index = list(rep(list(c(), times=600)));
index = list(rep(list(c(), times=600)));
index = list(rep(list(c(), times=length(allWords))));
index = list(rep(list(c(), times=length(allWords))));
length(allWords)
index = list(rep(list(c()), times=length(allWords)));
indexList = list(rep(list(c()), times=length(allWords)));
indexList(1)
indexList = list(rep(list(c()), times=length(allWords)));
indexList(1)
indexList[1]
indexList[1,2]
indexList[1,1]
indexList[[1,1]]
indexList[[i]][[j]]
indexList = list(rep(list(c()), times=length(allWords)));
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
if (is.element(allWords[[i]], newsArticles[[j]]))
indexList[[i]][[j]] = union(indexList[[i]][[j]],j)
}
}
indexList = list(rep(list(c()), times=length(allWords)));
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
if (is.element(allWords[[i]], newsArticles[[j]]))
indexList[[i]][[j]] <- union(indexList[[i]][[j]],j);
}
}
indexList[[1]][[1]]
indexList[[1]][[2]]
indexList[[2]][[2]]
indexList[[1]][[1]]
indexList[[1]][[1]] = union(indexList[[1]][[1]], 2)
indexList[[1]][[1]] = union(indexList[[1]][[1]], 3)
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
if (is.element(allWords[i], newsArticles[[j]]))
indexList[[i]][[j]] <- union(indexList[[i]][[j]],j);
}
}
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
i;
#if (is.element(allWords[[i]], newsArticles[[j]]))
# indexList[[i]][[j]] <- union(indexList[[i]][[j]],j);
}
}
a = 0;
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
a = a +1;
#if (is.element(allWords[[i]], newsArticles[[j]]))
# indexList[[i]][[j]] <- union(indexList[[i]][[j]],j);
}
}
a = 0;
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
a = a +1;
#if (is.element(allWords[[i]], newsArticles[[j]]))
# indexList[[i]][[j]] <- union(indexList[[i]][[j]],j);
}
}
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
if (is.element(allWords[[i]], newsArticles[[j]]))
indexList[[i]][[j]] <- union(indexList[[i]][[j]],j);
}
}
indexList[[1]][[1]] = 0;
indexList[[1]][[1]] = NULL
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
if (is.element(allWords[[i]], newsArticles[[j]]))
indexList[[i]][[j]] = union(indexList[[i]][[j]],j);
}
}
indexList = list(rep(list(c()), times=length(allWords)));
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
if (is.element(allWords[[i]], newsArticles[[j]]))
indexList[[1]][[i]] = union(indexList[[1]][[i]],j);
}
}
all = union(indexList, allWords);
all = c(indexList, allWords);
all = c(indexList[[1]], allWords);
allWords
all
indexList[[2]] <- allWords;
indexList[[2]]
indexList[[1]]
indexList[[3]]
indexList[[2]]
help match
?match
news1 = scan("news1.txt", what="character")
news2 = scan("news2.txt", what="character")
news3 = scan("news3.txt", what="character")
news4 = scan("news4.txt", what="character")
news5 = scan("news5.txt", what="character")
newsArticles = list(news1, news2, news3, news4, news5); # list of all articles
#list of all words
allWords = union(news1, news2);
allWords = union(allArticles, news3);
allWords = union(allArticles, news4);
allWords = union(allArticles, news5);
# make index list with 727 entries and 1 column set to NULL
indexList = list(rep(list(c()), times=length(allWords)));
# look through all words and set document ID
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
if (is.element(allWords[[i]], newsArticles[[j]])) # check if word matches article
{
indexList[[1]][[i]] = union(indexList[[1]][[i]],j); # set document ID to list
}
}
}
# set the second element to all the words
indexList[[2]]<- allWords;
# NOTE: list too long to be shown in Environment list
indexList[[1]] # see all document IDs
indexList[[2]] # see all words
indexList = list(rep(list(c()), times=length(allWords)));
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
if (is.element(allWords[[i]], newsArticles[[j]])) # check if word matches article
{
indexList[[1]][[i]] = union(indexList[[1]][[i]],j); # set document ID to list
}
}
}
indexList[[2,2]] # see all words
indexList[[2]] # see all words
indexList[[2]]<- allWords;
indexList[[2]] # see all words
match("of", indexList[[2]])
match("of", indexList[[3]])
a = match("of", indexList[[2]])
a
a = match("Taliban", indexList[[2]])
match("Taliban", indexList[[2]])
match("Monday", indexList[[2]])
match("Hundreds", indexList[[2]]) # position of word in total words
match("spokesman", indexList[[2]]) # position of word in total words
match("of", indexList[[2]]) # position of word in total words
match("of", indexList[[1]]) # position of word in total words
match("of", indexList[[2]]) # position of word in total words
match("spokesman", indexList[[2]]) # position of word in total words
match("of", indexList[[2]]) # position of word in total words
indexList[[1, 2]]
indexList(1, 2)
indexList[[c(1,a)]]
a = match("of", indexList[[2]]) # position of word in total words
indexList[[c(1,a)]]
a = match("Taliban", indexList[[2]]) # position of word in total words
indexList[[c(1,a)]]
news1 = scan("news1.txt", what="character")
news2 = scan("news2.txt", what="character")
news3 = scan("news3.txt", what="character")
news4 = scan("news4.txt", what="character")
news5 = scan("news5.txt", what="character")
newsArticles = list(news1, news2, news3, news4, news5); # list of all articles
#list of all words
allWords = union(news1, news2);
allWords = union(allArticles, news3);
allWords = union(allArticles, news4);
allWords = union(allArticles, news5);
# make index list with 727 entries and 1 column set to NULL
indexList = list(rep(list(c()), times=length(allWords)));
# look through all words and set document ID
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
if (is.element(allWords[[i]], newsArticles[[j]])) # check if word matches article
{
indexList[[1]][[i]] = union(indexList[[1]][[i]],j); # set document ID to list
}
}
}
# set the second element to all the words
indexList[[2]]<- allWords;
# NOTE: list too long to be shown in Environment list
indexList[[1]] # see all document IDs
indexList[[2]] # see all words
# find the dictionary entry that contains word X
position = match("Taliban", indexList[[2]]) # position of word in total words
# retrive the document ID in position X
indexList[[c(1,position)]]
rm(list=ls(all=TRUE))
news1 = scan("news1.txt", what="character")
news2 = scan("news2.txt", what="character")
news3 = scan("news3.txt", what="character")
news4 = scan("news4.txt", what="character")
news5 = scan("news5.txt", what="character")
newsArticles = list(news1, news2, news3, news4, news5); # list of all articles
#list of all words
allWords = union(news1, news2);
allWords = union(allArticles, news3);
allWords = union(allArticles, news4);
allWords = union(allArticles, news5);
# make index list with 727 entries and 1 column set to NULL
indexList = list(rep(list(c()), times=length(allWords)));
# look through all words and set document ID
for (i in 1:length(allWords)) # loop through all words
{
for (j in 1:length(newsArticles)) # loop through all articles
{
if (is.element(allWords[[i]], newsArticles[[j]])) # check if word matches article
{
indexList[[1]][[i]] = union(indexList[[1]][[i]],j); # set document ID to list
}
}
}
# set the second element to all the words
indexList[[2]]<- allWords;
# NOTE: list too long to be shown in Environment list
indexList[[1]] # see all document IDs
indexList[[2]] # see all words
# find the dictionary entry that contains word X
position = match("Taliban", indexList[[2]]) # position of word in total words
# retrive the document ID in position X
indexList[[c(1,position)]]
length(indexList)
